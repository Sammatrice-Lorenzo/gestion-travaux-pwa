name: Install Playwright browsers
description: Install Playwright browsers with cache (chromium only, no apt here)

inputs:
  browsers:
    description: Space-separated browser names to install
    required: false
    default: chromium

runs:
  using: composite
  steps:
    - name: Get Playwright version
      id: playwright-version
      shell: bash
      run: |
        VERSION=$(node -p "require('@playwright/test/package.json').version")
        echo "version=$VERSION" >> "$GITHUB_OUTPUT"

    - name: Cache Playwright browsers
      id: playwright-cache
      uses: actions/cache@v4
      with:
        path: ~/.cache/ms-playwright
        key: playwright-${{ runner.os }}-${{ steps.playwright-version.outputs.version }}-${{ inputs.browsers }}

    - name: Install Playwright browsers
      if: steps.playwright-cache.outputs.cache-hit != 'true'
      shell: bash
      env:
        PLAYWRIGHT_DOWNLOAD_CONNECTION_TIMEOUT: '600000'
      run: yarn playwright install ${{ inputs.browsers }}
